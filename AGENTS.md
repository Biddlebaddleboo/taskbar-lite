# AGENTS.md

- When installing an APK on a connected Android device, always install for user `0` only.
- Use `adb install -g --user 0` for APK installs unless the user explicitly asks for a different user or install mode.
